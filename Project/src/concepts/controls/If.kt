package concepts.controls

fun main(args: Array<String>){
    val grade: Double = 8.5

    if(grade >= 7.0) //with semicolon, higher or lower than 7.0 would still show the message because semicolon is a valid sentence
        println("Approved")
}